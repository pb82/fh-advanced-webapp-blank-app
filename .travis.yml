language: node_js
group: edge
sudo: required
services:
  - mongodb
  - docker
node_js:
  - "4.4.3"
  - "6.9.1"
before_install:
 - npm install -g grunt
 - npm install -g grunt-cli
 - npm install 
install: 
  - npm install fh-build -g
  - npm ls
  - fh-build template
  